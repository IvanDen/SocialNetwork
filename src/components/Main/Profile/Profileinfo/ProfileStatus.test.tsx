import React from "react";
import { create } from "react-test-renderer";
// import ProfileStatus from "./ProfileStatus";


describe("ProfileStatus component", () => {
    // test("StatusType from page should be in the state", () => {
    //     const component = create(<ProfileStatus status="Test status" />);
    //     const instance = component.getInstance();
    //     expect(instance.state.status).toBe("Test status");
    // });
    // test("after creation span should be displayed with correct status", () => {
    //     const component = create(<ProfileStatus status="Test status" />);
    //     const instance = component.root;
    //     const span = instance.findByType('span');
    //     expect(span).not.toBeNull();
    // });
    // test("after creation input should't be displayed", () => {
    //     const component = create(<ProfileStatus status="Test status" />);
    //     const instance = component.root;
    //     expect(() => {
    //         const input = instance.findByType('input');
    //     }).toThrow();
    // });
    // test("after creation span should be contains correct status", () => {dialogs
    //     const component = create(<ProfileStatus status="Test status" />);
    //     const instance = component.root;
    //     const span = instance.findByType('span');
    //     expect(span.children[0]).toBe("Test status");
    // });
    // test("input should be displayed in editMode instead of span", () => {
    //     const component = create(<ProfileStatus status="Test status" />);
    //     const instance = component.root;
    //     const span = instance.findByType('span');
    //     span.props.onDoubleClick();
    //     const input = instance.findByType('input');
    //     expect(input.props.value).toBe("Test status");
    // });
    // test("collback should be called", () => {
    //     const mockCollback = jest.fn();
    //     const component = create(<ProfileStatus status="Test status" updateStatus={mockCollback} />);
    //     const instance = component.getInstance();
    //     instance.deActivateEditMode();
    //     expect(mockCollback.mock.calls.length).toBe(1);
    // });
});
